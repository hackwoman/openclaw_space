# 2026-03-28

## WSL2 网络断连方案 B 落实
- Windows 10 build 19045.7058，Surface 不支持升级 Win11
- mirrored 模式不可用，改走方案 B
- **Windows 任务计划**：`WSL2-Wake-Recovery`
  - 监听系统电源事件（Power-Troubleshooter EventID=1 + Kernel-Power EventID=507）
  - 唤醒后执行 `wsl --shutdown`，重置网络栈
  - XML 配置：`C:\Users\kuikui workstation\Scripts\wsl-wake-task.xml`
- **WSL 保活脚本 v3** 升级：
  - 连续失败 3 次判定断网
  - 网络恢复时自动 `openclaw gateway restart`
  - 60 秒冷却防重复触发
  - 脚本路径：`scripts/wsl-network-keepalive.sh`

## 架构设计（全天迭代）
- 完成 ETL 平台架构设计文档：`.plans/etl-architecture-design.md`（~2200 行）
- 核心设计：Trace 调用链融合 CMDB（静态+动态关系统一）
- 存储统一 PostgreSQL 为核心（AGE 图扩展 + pgvector 向量扩展）
- 确立平台两大设计哲学：数据接入零人工 + AI 贯穿全层
- 数据接入优先级：日志 → Prometheus → 告警
- 规模目标：数百至数千台云主机
- 新增：网络/安全设备接入（Syslog/SNMP/NetFlow + LLM 自动解析）
- 新增：5 大架构约束原则（解耦/可替换/时间线统一/CMDB统一/自定义标签）
- 新增：数据可靠性设计（全链路 At-Least-Once + 边缘缓冲 + 间隙检测）
- 新增：数据时效分层（HOT/WARM/COOL/COLD 四级 + 时效路由）
- 新增：图计算独立服务（实时融合 + 批量计算 + 双维度图模型）
- 新增：端到端可观测（浏览器RUM/Mobile SDK → 后端 → 基础设施，trace_id 贯穿）
- 新增：AI 诊断引擎（5步流水线 + 4种算法综合根因推理 + 概率排序）
- 新增：多租户RBAC权限（标签隔离 + 数据范围过滤）
- 新增：数据生命周期管理（HOT/WARM/COLD/ARCHIVE 分层存储）
- 新增：平台自我监控（元监控与业务监控物理隔离）
- 新增：安全架构 + 插件扩展机制
- 主人强调：架构解耦、模块可迭代、数据一致性、CMDB统一、自定义标签
- 主人提出不急于速度，周详为主
- 主人提出告警革命性思路：业务风险度驱动，而非技术严重度
- 风险度引擎提升为独立核心服务（与 CMDB/ETL 并列）
- 平台三大核心价值确立：零人工 + AI 贯穿 + 业务风险驱动
- 风险度 = 技术严重度 × 传播距离 × 影响面（CMDB 拓扑驱动）
- AI 模块设计为可插拔大脑（Brain Adapter），支持 OpenAI/Claude/千问/本地模型
- 不同场景可配不同大脑，租户级配置，一键切换
- Meta-Agent 大管家架构：自主监控、按需产生 Sub-Agent、主动进化
- 平台四大核心价值确立：零人工 + AI 贯穿 + 业务风险驱动 + AI 自主进化
- 架构文档最终 ~3000 行，80+ 子章节
