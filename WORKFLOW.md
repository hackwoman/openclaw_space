# WORKFLOW.md - Lily 开发工作流

基于 PIV 循环（Prime → Implement → Validate）的 AI 编码工作流规范。

---

## 🔄 PIV 循环

任何开发任务都遵循三个阶段，按顺序执行，不可跳过。

### Phase 1: Prime（加载上下文）

**目标：** 在动手之前，全面理解项目现状。

**执行步骤：**

1. **分析项目结构**
   - 列出目录结构（tree -L 3）
   - 识别主要语言、框架、构建工具

2. **读核心文档**
   - README、CLAUDE.md、AGENTS.md
   - 架构文档、API 文档
   - 相关的 .claude/reference/ 文档

3. **识别关键文件**
   - 入口文件（main.py、index.ts 等）
   - 配置文件（pyproject.toml、package.json）
   - 核心 model/schema 定义
   - 重要的 service/controller 文件

4. **了解当前状态**
   - 最近 10 次 git 提交
   - 当前分支和工作区状态
   - 最近的开发重点

**输出：** 一份简洁的项目概览报告（可以口述，不需要写文件）

---

### Phase 2: Plan（规划实现）

**目标：** 创建详细的实施计划，**不写任何代码**。

**核心原则：Context is King**
- 计划必须包含所有实现所需的上下文
- 另一个开发者（或未来的我）只看计划就能一次实现成功
- 每个任务都原子化、可独立验证

**执行步骤：**

1. **理解需求**
   - 提取核心要解决的问题
   - 确定功能类型：新功能 / 增强 / 重构 / 修复
   - 评估复杂度：低 / 中 / 高

2. **代码库调研**
   - 搜索代码库中类似的实现
   - 识别编码规范（命名、文件组织、错误处理）
   - 找出要复用的 pattern
   - 标注反模式

3. **外部文档研究**
   - 查阅相关库的最新文档
   - 找到具体的实现示例
   - 识别常见坑和已知问题

4. **深度思考**
   - 这个功能如何融入现有架构？
   - 关键依赖和执行顺序是什么？
   - 可能出什么问题？（边界情况、竞态条件、错误处理）
   - 性能和安全考虑？

5. **生成计划文件**
   - 路径：`.plans/{YYYY-MM-DD}-{kebab-case-name}.md`
   - 使用 PLAN_TEMPLATE.md 模板

**关键要求：**
- ✅ 每个任务都有可执行的验证命令
- ✅ 任务按依赖顺序排列，从上到下执行
- ✅ 包含具体的文件路径和行号引用
- ✅ 标注 GOTCHA（已知坑点）

---

### Phase 3: Validate（验证）

**目标：** 确保实现正确，零回归。

**执行步骤：**

1. **语法和风格检查**
   - Lint（ruff check / eslint）
   - 格式化检查

2. **单元测试**
   - 运行全部单元测试
   - 检查覆盖率（目标 ≥ 80%）

3. **集成测试**
   - 运行集成测试
   - 测试 API 端点

4. **构建验证**
   - 前端构建（npm run build）
   - 后端启动测试

5. **手动验证**
   - API 健康检查
   - 关键功能手动测试

6. **代码审查**
   - 审查变更文件
   - 修复发现的问题

7. **生成报告**
   - 测试通过/失败数
   - 覆盖率
   - 整体健康评估（PASS/FAIL）

---

## 📝 Commit 规范

### 格式

```
<tag>: <简短描述>

<可选：详细描述>
```

### 标签

| 标签 | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 bug |
| `docs` | 文档变更 |
| `style` | 代码格式（不影响逻辑） |
| `refactor` | 重构（不改变功能） |
| `perf` | 性能优化 |
| `test` | 添加/修改测试 |
| `chore` | 构建/工具变更 |
| `config` | 配置变更 |

### 原则

- **一个 commit = 一个逻辑变更**
- 先 `git status && git diff` 查看变更
- `git add` 精确选择文件
- 提交信息清晰、具体

---

## 📄 PRD 模板

需要产品需求文档时，使用 `PLAN_TEMPLATE.md` 中的 PRD 部分，或创建独立 PRD。

标准章节：
1. 执行摘要
2. 目标用户
3. MVP 范围
4. 用户故事
5. 技术栈
6. 实施阶段
7. 成功标准
8. 风险与缓解

---

## ⚠️ 流程红线

1. **不跳过 Prime 阶段** — 哪怕"看起来简单"也要先搞懂上下文
2. **Plan 阶段不写代码** — 规划和实现严格分离
3. **不做完不罢休** — Validate 阶段所有命令必须通过
4. **不批量提交** — 每个逻辑变更独立 commit
5. **不确定就问** — 需求模糊时先澄清，不要猜

---

_基于 hackwoman/habit-tracker 项目的 PIV 工作流适配_
_创建于 2026-03-27_
